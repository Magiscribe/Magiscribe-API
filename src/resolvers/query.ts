const predictions = [
  {
    prompt: "The next big thing in tech will be",
    context: "the next big thing in tech will be",
    result: "a new type of AI that can predict the future"
  }
];

const query = {
  predictions: () => predictions,
}

export default query;